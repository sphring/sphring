language: php
php:
  - 5.5
  - 5.6
  - hhvm
install:
  - composer install
script: phpunit --bootstrap tests/bootstraptravis.php --no-configuration tests --coverage-clover clover.xml
after_script:
  - cat clover.xml
  - php vendor/bin/coveralls -v
